# 美颜方案评估：现有实现优化 + 开源库接入

> 评估日期：2026-09-23
> 评估对象：当前纯 shader 美颜链路（v2.0.158）
> 结论性质：**技术评估，未改动任何代码**

---

## 摘要（TL;DR）

| 问题 | 结论 |
|---|---|
| 要不要引入开源美颜库？ | **暂不引入**。理由：① 唯一真开源选项 GPUPixel 与鸿蒙迁移战略冲突；② 我们的痛点不在"缺功能"，而在"算法实现方式"；③ 按 `AI_MIGRATION_PLAN.md` 要求，GPUPixel 必须 POC 验证，禁止凭猜测接 |
| 优化重点在哪？ | **妆容区域形状化**（性价比最高，改动小、收益大）→ 磨皮算法升级 → 美白/混合模式 |
| 最大发现 | 所有妆容区域判定都基于「**以关键点为圆心的正圆**」，而嘴唇/眉毛/腮红/眼袋都是**非圆形**，这是观感生硬的**根本原因** |

**优先级建议**（详见第三节）：

| 序 | 优化项 | 改动量 | 观感收益 | 风险 |
|---|---|---|---|---|
| ① | 妆容区域：圆形 → 椭圆/胶囊 mask | 中 | ★★★★★ | 低 |
| ② | 磨皮：单尺度双边 → 导向滤波 / 频域分离 | 中 | ★★★★☆ | 中 |
| ③ | 美白：加法 → 肤色 LUT（保护高光） | 小 | ★★★★☆ | 低 |
| ④ | 妆容混合：加法 → soft-light | 小 | ★★★☆☆ | 低 |
| ⑤ | 磨皮后补 USM 锐化（恢复通透感） | 小 | ★★★☆☆ | 低 |
| ⑥ | 变形：径向位移 → MLS 网格 warp | 大 | ★★★☆☆ | 高 |

---

## 一、现状盘点（基于代码实测，非文档描述）

### 1.1 架构

| 层 | 实现 | 位置 |
|---|---|---|
| 人脸检测 | MediaPipe `face_landmarker.task`（**478 点**，VIDEO 模式） | `MediaPipeFaceManager.kt` |
| 兜底检测 | Android `FaceDetector`（x86 模拟器无 MediaPipe native 库时） | 同上 |
| 效果渲染 | **纯 GLSL**，内嵌在 `VRGLRenderer.kt` 的着色器字符串里 | `VRGLRenderer.kt` |
| 上下文 | **OpenGL ES 2.0**（`setEGLContextClientVersion(2)`） | `VRGLSurfaceView.kt` |
| 采样 | 每 8 帧回读单眼视口中心 512×512 → 后台检测 | `VRGLRenderer.maybeScheduleFaceSampling()` |

**传给 shader 的只有 8 个锚点**：`faceCenter` / `eyeDistance` / `eyeLeft` / `eyeRight` / `mouth` / `chin`
（478 点仅在 Kotlin 侧用于推导这 8 个值，**没有传给 GPU**）—— 这点很关键，是第 ① 项优化要突破的瓶颈。

### 1.2 各效果的当前实现

| 效果 | 实现方式 | 代码位置 | 评价 |
|---|---|---|---|
| 磨皮 | 8 邻域**双边滤波**（步长 3 像素）+ 肤色判断，混合强度 0.90 | `VRGLRenderer.kt:646-687` | 单尺度，半径有限 |
| 美白 | `color.rgb += vec3(uWhitening * 0.12)` | `:713-715` | ⚠️ **纯加法** |
| 去黑眼圈 | `color.rgb += vec3(uDarkCircles * 0.13)`，圆形区域 `r < 0.35*fUnit` | `:718-724` | ⚠️ 加法 + 圆形 |
| 眉毛 | `mix(color, color*0.55, …)`，圆形区域 `r < 0.32*fUnit` | `:727-736` | ⚠️ 圆形，涂不出眉形 |
| 口红 | 三通道分别 `mix(…, 0.9 / 0.15 / 0.3, f)`，圆形区域 `r < 0.35*fUnit` | `:738-745` | ⚠️ 圆形，会漏嘴角/溢出 |
| 腮红 | `color.rgb += vec3(0.12,0.04,0.05) * …`，圆形区域 `r < 0.55*fUnit` | `:747-757` | ⚠️ 加法 + 圆形 |
| 牙齿 | 圆形区域 `r < 0.22*fUnit` 内提亮，`luma > 0.45` 才作用 | `:759-767` | 圆形（嘴腔近似尚可） |
| 瘦脸/大眼/鼻/嘴 | `factor = (1 - d/(k*fUnit)) * 参数 * 系数` 的**径向位移** | `:582-630` | 边界连续性一般 |
| 长腿 | `normY -= (normY - 0.05) * uLongLegs * 0.08` 线性拉伸 | `:582` | 简单但可用 |

### 1.3 三个结构性问题

**问题 1：妆容区域全是正圆**（最影响观感）

```glsl
// 口红：一个以嘴中心为圆心的正圆
float distLip = distance(tc, mouthC);
if (distLip < 0.35 * fUnit) { … }
```

但嘴唇是**横向长条**（宽 ≈ 2.5× 高，且有倾角）、眉毛是**细长斜条**、腮红是**大椭圆**、眼袋是**月牙形**。
用正圆近似的后果：
- 圆太小 → 嘴角/眉尾涂不到（颜色断在中间）
- 圆太大 → 涂到下巴、鼻翼、脸颊
- 且**完全不跟随表情与头部姿态**（张嘴/侧脸时更明显）

**问题 2：提亮类效果用加法**

```glsl
color.rgb += vec3(uWhitening * 0.12);   // 美白
color.rgb += vec3(uDarkCircles * 0.13); // 去黑眼圈
color.rgb += vec3(0.12, 0.04, 0.05) * …; // 腮红
```

加法提亮的副作用：**高光区直接溢出**（鼻尖/额头出现色块）、**暗部提亮不足**（黑眼圈是"暗"不是"缺亮度"）。
成熟做法：美白走**肤色 LUT / 曲线**，腮红走 **soft-light 或 multiply 混合**。

**问题 3：磨皮是单尺度双边**

双边滤波能保边，但：**去不掉色差大的孤立点**（痘痘/黑痣顶端的小白点），且大半径时计算量线性上升（8 邻域只有 ±3 像素覆盖）。
商业级做法是 **频域分离**（低频平滑色块 + 高频保留毛孔）或 **导向滤波**（O(N) 线性时间、质量接近双边）。

---

## 二、开源方案横向评估

### 2.1 结论表

| 方案 | 是否真开源 | 许可 | 平台 | 体积/性能 | 对我们的适配性 |
|---|---|---|---|---|---|
| **GPUPixel** | ✅ 真开源（源码完整） | MIT | iOS/Android/Mac/Win/Linux | AAR **2.1MB**；1080p 约 **5~6ms / CPU 3~5%** | ⚠️ 与鸿蒙战略冲突，需 POC |
| **PixelFree / PixelFreeEffects** | ❌ **不是开源库** | 仓库有 LICENSE，但**含 `.lic` 授权文件** | iOS/Android/**HarmonyOS**/Win/mac | 未知（核心是二进制） | ❌ 不可用（见 2.3） |
| Meihu-Beautyface-sdk | 部分 | — | iOS 为主 | — | ❌ Android 不完善 |
| BeautifyFaceDemo | ✅ 示例代码 | — | Android(GPUImage) | — | 📖 仅作算法参考 |
| android-gpuimage | ✅ | Apache | Android | — | ❌ 通用滤镜库，**不含**磨皮/瘦脸算法 |
| MediaPipe Face Effects/Stylizer | ✅ | Apache | Android | 已有依赖 | ❌ 做贴纸/风格化，**不做美颜** |

### 2.2 GPUPixel：唯一真正可用的开源美颜库

**优点**
- 功能对口：磨皮 / 美白 / 瘦脸 / 大眼 / 口红 / 腮红（正是我们要的）
- **体积极小**：Android AAR 仅 2.1MB
- 性能优秀：官方数据 1080p 约 5~6ms（小米 10 / 华为 Mate30）
- C++11 + OpenGL ES，与我们的 GLES 管线同源

**关键风险（必须在 POC 中验证）**
1. **人脸检测依赖**：早期版本用 **Face++（需网络验证）**；v1.3.0-beta 起改用 **Mars-Face**（本地）。
   → 我们已有 MediaPipe 478 点，若接入 GPUPixel，要么接受它自带检测（重复劳动 + 可能效果更差），
   要么做**桥接**（把它的人脸检测换成喂我们的关键点）—— 后者需要改其 C++ 源码。
2. **EGL 上下文协调**：GPUPixel 自建 `GPUPixelContext`。我们已有 `VRGLRenderer` 的 EGL 上下文，
   两者共享纹理需要**同一 context 或 shared context**，接入不当会出现"黑屏/纹理丢失"。
3. **输入格式转换**：GPUPixel 期望 RGBA/YUV 纹理；我们用的是 **OES 视频纹理**（SurfaceTexture）
   → 需要多一次转换 pass（1080p 下是有成本的）。
4. **鸿蒙迁移**：这是**最大冲突**。`DEPENDENCY_MAP.md` 已明确：
   > Harmony 路径：美颜 **优先移植现有 GLSL**（跨端可复用），GPUPixel 降级为备选
   引入 C++ NDK 库后，鸿蒙侧需**重新适配**（GLSL 能移植，C++ 库不能）。

### 2.3 ⚠️ 重要纠正：PixelFree 不是开源库

多个技术博客把它列为「开源 / MIT 友好」，**这inaccurate**。实测其仓库：

- 仓库内容只有 **各平台 DEMO 工程**（`SMBeautyEngine_android/iOS/harmonyOS/...`）
- 存在 **`pixelfreeAuth.lic` 授权文件** 与 `SMUpdateLicService`（许可证更新服务）
- README 自述：**「SDK 相机调色可免费使用，其他功能需付费」**（原文）

⇒ 它是**商业 SDK 提供公开 Demo**，不是可自由使用的开源实现。
**不要**基于"支持 HarmonyOS"就把它纳入方案（那些 Demo 里的核心库仍是收费二进制）。

---

## 三、优化项详解（自研路线）

### ① 妆容区域：正圆 → 椭圆 / 胶囊 mask ⭐ 最高性价比

**为什么最优先**：改动集中在"多传几个几何参数 + shader 里换距离函数"，
不需要动架构、不影响性能，却是**观感提升最大**的一项（妆容从"一团颜色"变成"贴合五官"）。

**做法**

1. **Kotlin 侧**（`MediaPipeFaceManager`）从 478 点算出**形状参数**，扩展现有 `FaceResult`：

   | 新增字段 | 来源（MediaPipe 点索引） | 用途 |
   |---|---|---|
   | `mouthWidth` / `mouthHeight` / `mouthAngle` | 嘴唇外轮廓 20 点（61-67, 291-295 等） | 口红椭圆 |
   | `browLeftStart/End` / `browRightStart/End` | 眉 10 点（70,63,105,66,107 / 336,296,334,293,300） | 眉毛胶囊 |
   | `cheekLeftCenter` / `cheekRightCenter` / `cheekRadius` | 下颚 + 颧骨点推导 | 腮红椭圆 |
   | `jawWidth`（脸宽） | 234 / 454（左右脸缘） | 瘦脸范围、归一化尺度 |
   | `eyeLeftWidth/Height` / 同右 | 眼 16 点（33,133 / 362,263） | 大眼椭圆（**取代正圆 → 更自然**） |

2. **Shader 侧**：把"圆距离"换成**椭圆距离**（含旋转）：

   ```glsl
   // 椭圆有符号距离（近似）：p 为相对椭圆中心的坐标，a/b 为半轴，θ 为倾角
   float ellipseMask(vec2 p, float a, float b, float theta) {
       float c = cos(theta), s = sin(theta);
       vec2 q = vec2(c * p.x + s * p.y, -s * p.x + c * p.y);
       float d = length(vec2(q.x / a, q.y / b));
       return clamp(1.0 - d, 0.0, 1.0);   // 0~1 的 soft mask
   }
   ```
   眉毛用**胶囊距离**（线段 + 半径）：

   ```glsl
   float capsuleMask(vec2 p, vec2 a, vec2 b, float r) {
       vec2 pa = p - a, ba = b - a;
       float h = clamp(dot(pa, ba) / dot(ba, ba), 0.0, 1.0);
       return clamp(1.0 - length(pa - ba * h) / r, 0.0, 1.0);
   }
   ```

3. **顺带收益**：mask 从"硬边界"变成**软权重（0~1）**，边界羽化，不再有生硬的圆环痕迹；
   且椭圆参数**随关键点实时更新** → 张嘴、侧脸时妆容跟着变形。

**代价**：`MediaPipeFaceManager` 增加约 60~80 行几何计算；shader 增加 2 个函数 + 约 8 个 uniform；
新增 uniform 数量在 GLES2 限制内（当前已用约 30 个，上限通常 100+）。

---

### ② 磨皮：单尺度双边 → 导向滤波 / 频域分离

**现状问题**：8 邻域双边（±3 像素）只能抹平细节，**对痘痘色块（低频瑕疵）无效**；
逼近方式是提高混合强度（0.90）→ 结果是"糊"而不是"净"。

**两条可选路径**

| 路径 | 原理 | 成本 | 效果 | 建议 |
|---|---|---|---|---|
| **频域分离** | `Low = blur(src)`，`High = src - Low`；只对 Low 做平滑（可多次小半径），再 `Low' + High` | 需要 1 次大半径模糊（可用 2-pass 高斯降采样，成本可控） | ★★★★★ 保纹理最好 | **推荐**（还能复用到"美白"的亮度分离） |
| **导向滤波** | 以原图为引导图，`a = var/(var+ε)`, `b = mean - a*mean` | 需要 2 次 box blur（均值滤波在 GPU 上可用 2-pass 积分图或多次采样） | ★★★★☆ O(N) 线性 | 备选 |

**推荐实现（频域分离 + 双边）**：
```
低频 = 4-tap 降采样模糊(src, 半径≈2% 画面宽)
高频 = src - 低频
低频' = 双边滤波(低频)          ← 现在这一步是必然有效的（低频层没有纹理可糊）
结果 = 低频' + 高频 * k(k≈0.7~1.0 可调"纹理保留度")
```
这样"磨皮强度"与"纹理保留度"**解耦成两个参数**，用户能调出"干净但还有质感"。

---

### ③ 美白：加法 → 肤色 LUT

**现状问题**：`color.rgb += vec3(0.12 * uWhitening)` → 鼻尖、额头等高光区直接溢出成白块；
且暗部提亮是线性的，肤色会发灰。

**做法**（两种，任选）
- **轻量**：按亮度做**保高光提亮**（类似 Photoshop 的 "Screen → Luminosity"）
  ```glsl
  vec3 w = color.rgb + uWhitening * 0.12 * (1.0 - color.rgb); // 越亮加得越少
  ```
  仅此一行就能显著减少高光溢出 ✓ **极低成本**
- **进阶**：肤色 LUT 贴图（一张 16×16×16 或 2D 条带）→ 把肤色映射到目标色域，
  可同时做"提亮 + 去黄 + 冷白皮"，这也是商业 SDK 的标准做法；

**建议**：先做轻量版（1 行），若用户想要"冷白皮/粉白"再上 LUT。

---

### ④ 妆容混合：加法 → soft-light / multiply

```glsl
// 现在（腮红）
color.rgb += vec3(0.12, 0.04, 0.05) * intensity;

// 建议：soft-light 混合（保留原有明暗层次）
vec3 blush = vec3(0.85, 0.45, 0.5);
color.rgb = mix(color.rgb, softLight(color.rgb, blush), intensity * 0.6);
```
同理眉毛（现在是 `color*0.55` 变暗，偏硬）可改为 multiply + 少量柔化；口红已是 mix，方向正确 ✓（但区域要换成椭圆）。

---

### ⑤ 磨皮后补 USM 锐化

磨皮后整体会"发闷"，商业做法是**再用一次锐化把五官边缘找回来**：
```glsl
vec3 detail = src - blur(src, 小半径);
color.rgb += detail * uSharpen * 0.3;
```
可复用频域分离里的高频层（**零额外成本**）✓

---

### ⑥ 变形：径向位移 → MLS 网格 warp

**现状**：`factor = (1 - d/(k*fUnit)) * 参数 * 系数`，位移沿径向、权重线性衰减。
问题：多个效果叠加时（瘦脸+小头+大眼）**会互相干扰**，大角度侧脸时可能产生**撕裂/褶皱**。

**成熟做法**：MLS（Moving Least Squares）或三角形网格 warp —— 用 478 点的网格做约束变形。
**代价高**（需要在 GPU 上传网格 + 解线性系统，或预计算位移场），
**建议放到最后**，且只在"瘦脸效果无法接受"时才做。

---

## 四、若决定接入 GPUPixel：POC 清单

> 依据 `AI_MIGRATION_PLAN.md`：**"GPUPixel 是否能够稳定运行于目标 HarmonyOS NEXT 环境，必须 POC 验证。
> 禁止 AI 根据网络文章或猜测直接判断。"** 同理，Android 侧也建议先 POC。

**POC 目标（最小验证）**

1. **上下文兼容**：GPUPixel 的 `GPUPixelContext` 能否与现有 `VRGLRenderer` 的 EGL 上下文**共享纹理**？
   - 验证方式：把一帧 OES 视频纹理转成 RGBA → 交给 GPUPixel → 取回 → 显示；确认无黑屏/无拷贝回流
2. **人脸检测方案**：用 GPUPixel 自带的 Mars-Face，还是把我们的 MediaPipe 478 点桥接进去？
   - 倾向**桥接**（复用已验证的坐标换算 + 保留我们 11 种效果的一致性）
   - 需评估改动其 C++ 源码的工作量
3. **性能实测**：1080p/60fps 下加入 GPUPixel 后的**帧时间增量**（目标 < 3ms）
4. **VR 分屏**：左右眼两次绘制时的调用方式（GPUPixel 是否支持复用同一结果）
5. **体积**：AAR 2.1MB + Mars-Face 模型体积
6. **许可合规**：MIT 全文入库 + 保留版权声明

**POC 的替代收益**：即使最终不接入，POC 过程也能确认"我们的 shader 与商业级实现的差距具体在哪"。

---

## 五、路线图与实施状态

> **2026-09-23 更新**：阶段 1 与阶段 2 已实施（v2.0.159）；阶段 3 作为**远期规划**保留，本次不动。

```
阶段 1（低风险）                                    ✅ v2.0.159 已完成
  ├─ ③ 美白改保高光提亮      color += w * (1 - color)
  ├─ ④ 腮红/眉毛/口红 → soft-light / 柔化 multiply + smoothstep 羽化
  └─ ⑤ 磨皮后补 USM           由「皮肤质感」参数 <1> 统一控制（见阶段 2 ②）

阶段 2（核心收益）                                  ✅ v2.0.159 已完成
  ├─ ① 妆容区域形状化：新增 ellipseMask() 椭圆软遮罩，
  │     FaceResult 扩展 mouthHalfWidth / mouthHalfHeight / mouthAngle，
  │     口红 / 眉 / 腮红 / 眼袋 / 牙齿 共 5 处由「正圆」改为「贴合形状的椭圆」
  └─ ② 磨皮改频域分离：低频（大半径保边均值）+ 高频（原图−低频），
        新增 uTextureDetail（皮肤质感，0.5~1.3）把「磨皮强度」与「纹理保留度」解耦

阶段 3（远期规划，本次未实施）
  ├─ ⑥ 变形改 MLS 网格          仅当瘦脸/大眼观感仍不达标时才做（代价高）
  ├─ 大眼变形的权重场仍用正圆     （妆容已椭圆化；变形段涉及 tc 位移，改动风险高，暂缓）
  ├─ 肤色 LUT（冷白皮/粉白等）   需 2D 条带贴图（GLES2 无纹理数组）
  └─ GPUPixel POC                仅当：鸿蒙路线确认需要 / 或要求达到商业级效果
       前置条件：按 AI_MIGRATION_PLAN.md 必须先 POC，禁止凭猜测接入
```

### 实施后仍需注意

- **GLSL 语法错误只在运行时暴露**（Kotlin 编译抓不到）→ 改 shader 后必须在真机确认画面正常（不要黑屏/无效果）
- 新增 uniform 若被编译器优化掉，`glGetUniformLocation` 返回 -1，此时 `glUniform*` 静默忽略 ✓ 不会崩
- 本版仍**未做**：大眼变形椭圆化、MLS 变形、肤色 LUT、GPUPixel

**为什么不建议现在就上 GPUPixel**：
1. 我们的痛点（区域形状、混合模式、磨皮尺度）**不是功能缺失**，而是实现方式 —— 换库不一定解决
2. 会**破坏跨端复用**（鸿蒙侧要重做），与既有迁移规划冲突
3. 引入 NDK 依赖后，构建复杂度、x86 模拟器兼容性、长期维护成本都上升
4. 按迁移计划要求，**必须先 POC**，不是"接了就完"

---

## 六、验证方式（改动后如何确认变好）

| 项 | 验证方法 |
|---|---|
| 妆容形状 | 正面/侧脸/张嘴 三种状态截图，确认口红贴合唇形、眉毛成条、腮红边界柔和 |
| 美白 | 观察鼻尖/额头是否还有白块；对比改动前后同一帧 |
| 磨皮 | 放大到 100% 看毛孔是否保留；对比"高强度磨皮"是否仍有皮肤质感 |
| 性能 | `adb shell dumpsys gfxinfo` 或帧时间日志；目标 60fps 不掉帧 |
| 一致性 | 分屏 VR 下左右眼效果一致；4 种投影模式下无异常 |

⚠️ **注意**：MediaPipe 在 x86_64 模拟器不可用（走 FaceDetector 兜底），
**涉及坐标/形状的改动必须在真机上验收**。

---

## 附：GLES2 的客观制约（影响部分优化选型）

| 能力 | GLES2 | 影响 |
|---|---|---|
| PBO（异步回读） | ❌ | 人脸采样只能同步 `glReadPixels`（现状已用"减少采样 + 复用缓冲"缓解） |
| 3D 纹理 / 纹理数组 | ❌ | 肤色 LUT 只能用 2D 条带贴图 |
| `textureLod` / 多级模糊优化 | 受限 | 频域分离的大半径模糊需靠"降采样 + 2-pass"实现 |
| 浮点渲染目标 | 需扩展 | 频域分离若用半浮点会更精确，需检查 `OES_texture_half_float` 支持 |

**是否升级 GLES3**：能解锁 PBO 与浮点 RT，但要检查目标设备覆盖率与驱动兼容性；
**建议暂不升级**（当前瓶颈不在这些特性上）。
