*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
debug.keystore

# v106：release 签名密钥（绝不可提交！泄露即丢失签名身份）
my-upload-key.jks
keystore.properties
*.jks

# v107：Firebase 配置（含项目标识，勿提交公开仓库；放置后构建自动启用统计）
google-services.json

# v117：Android 构建产物。注意 `/build` 只匹配根目录，app/build 可达数 GB，
# 必须单独忽略，否则首次提交会把整个构建目录（含 400MB+ APK）塞进仓库。
/app/build

# v117：本项目约定「改动前留备份」(.bak)，备份属本地安全网，不入仓
*.bak*

# v117：WorkBuddy 本地工作数据（含本机绝对路径与个人记忆），不入仓
.workbuddy/

# v2.0.127：内置 ASR 模型（model.int8.onnx 228MB）不入库——
# 超过 GitHub 单文件 100MB 硬限制，push 会被整包拒绝。
# 改为构建前用脚本拉取：`python scripts/fetch_asr_model.py`
# 注：tokens.txt 仅 0.3MB，随仓库提交，保证 clone 后目录结构完整。
app/src/main/assets/sense-voice/model.int8.onnx
