# Aura face VR Player — 代码健康度审查报告

- **审查日期**：2026-09-12
- **审查版本**：v1.0.116（未修改任何源码，纯只读审查）
- **审查范围**：`app/src/main/java/com/example/` 全部 34 个 Kotlin 文件，共 14,119 行
- **审查方式**：静态代码审查 + 关键前提实测验证
- **背景**：真机上刚暴露过一个音频解码缺陷（MP2 软解，已单独修复），因此本次做一轮整体排查

> **可信度标记说明**
> - ✅ **已核实** — 本次审查中由我亲自读源码/实测确认，可直接修
> - ⚠️ **疑似** — 依据充分但未实测复现，建议修前先验证

---

## 一、P0 级：功能性失效或崩溃（建议优先修）

### 1. ✅ 全部 LUT 调色滤镜静默失效

**位置**：`vr/LutUtils.kt:55-56`

```kotlin
val lower = t.lowercase()
...
if (lower.startsWith("lut_3d_size")) {
    size = t.substringAfter("lut_3d_size").trim().toInt()   // ← 用了原始行 t
    continue
}
```

**问题**：判断用 `lower`（小写化），取值却用 `t`（原始行）。项目自带的 12 个 `.cube` 资产文件里写的都是**大写**：

```
TITLE "Classic Cyan-Orange Style"
LUT_3D_SIZE 33
```

`"LUT_3D_SIZE 33".substringAfter("lut_3d_size")` 因大小写不匹配找不到分隔符，Kotlin 会返回原字符串本身（`missingDelimiterValue` 默认为 `this`），于是执行 `"LUT_3D_SIZE 33".trim().toInt()` → 抛 `NumberFormatException`。

**影响**：调用点 `VRPlayerScreen.kt:4613` 用 `catch (e: Exception)` 只打日志，用户点任何一个内置 LUT 都**毫无反应且无任何提示**。自定义导入的 `.cube`（`VRPlayerScreen.kt:1463`）只要用 Adobe 标准大写写法同样失败。LUT 功能整体不可用。

**建议**：改为 `t.substringAfter("lut_3d_size", "").trim()`，或统一用正则 `Regex("(?i)lut_3d_size\\s+(\\d+)")` 提取。`DOMAIN_MIN/MAX`（59-66 行）有同样写法，需一并修。

---

### 2. ✅ MediaPipe 468 点精细人脸特征从未生效

**位置**：`vr/MediaPipeFaceManager.kt:214`（定义）与 `:129-142`（构造）

```kotlin
// 214 行：默认值
val hasDetailedLandmarks: Boolean = false

// 129-142 行：MediaPipe 成功分支构造 FaceResult 时未传该参数
return FaceResult(
    detected = true,
    centerX = centerX,
    ...
    chinY = chin.y()
)
```

**问题**：全项目 grep 后确认，`hasDetailedLandmarks` **没有任何一处被赋值为 `true`**。而渲染侧依赖它：

```kotlin
// VRGLRenderer.kt:1464
if (result.hasDetailedLandmarks) {
    hasDetailedLandmarks = 1
    eyeLeftXUniform = ...
```

因此 `hasDetailedLandmarks` 恒为 `0` → shader uniform `uHasDetailed` 恒为 0 → 永远走 `fCenter = vec2(0.5, 0.45)` 的硬编码粗略锚点。

**影响**：MediaPipe 付出的检测成本（眼睛/嘴/下巴精确点位）**全部被丢弃**，美颜/形变只能靠粗略中心对齐，跟随精度远低于设计预期。

**建议**：在 `129-142` 行的构造里补 `hasDetailedLandmarks = true`（并确认各点位均已赋值有效）。

---

### 3. ✅ Android 7/8/9 上必然崩溃（API 级别未判断）

**位置**：`vr/DecoderCapabilities.kt:29`、`vr/ExperimentalDecode.kt:38-40`

```kotlin
for (info in list.codecInfos) {
    if (info.isEncoder || !info.isHardwareAccelerated) continue   // ← API 29 方法
```

**问题**：`MediaCodecInfo.isHardwareAccelerated()` 是 **API 29（Android 10）** 才引入的方法，而本项目 `app/build.gradle.kts:17` 声明 `minSdk = 24`。在 Android 7/8/9 上调用会抛 `NoSuchMethodError`。

关键点：**这是 `Error` 而非 `Exception`**，所以 `ExperimentalDecode.kt` 外层即使有 `catch (e: Exception)` 也拦不住；而 `DecoderCapabilities.kt:29` 的这行**根本没有被 try 包裹**（try 从 32 行才开始）。

**触发路径**：`VRPlayerScreen.kt:2120`（打开 8K 视频时）、`VRPlayerScreen.kt:1980`（开启"强制硬解"开关时）。

**建议**：`if (Build.VERSION.SDK_INT >= 29 && info.isHardwareAccelerated)`，低版本用 `info.name.startsWith("OMX.") && !name.contains("google")` 之类的启发式替代。

---

### 4. ✅ 在线字幕"已下载并加载"是假提示

**位置**：`vr/SubtitleSettingsPanel.kt:113`（默认空实现）与 `:382`（调用）

```kotlin
// 113 行：参数默认值是空实现
onSubtitleFileLoaded: (File) -> Unit = {},

// 382 行：下载成功后调用
searchStatus = "已下载并加载"
onSubtitleFileLoaded(file)
```

**问题**：`VRPlayerScreen.kt:4355` 的调用点**没有传** `onSubtitleFileLoaded`，于是落到默认空实现。字幕文件下载成功、界面提示"已下载并加载"，但**文件从未被解析、字幕永远不会显示**。

同一处调用还漏传了 `maxLines` / `onMaxLinesChange`（默认 `2` / 空实现），导致设置面板里"每段行数"控件**改了没反应、永远显示 2 行**。

**建议**：在调用点补齐这两个回调；`onSubtitleFileLoaded` 接到现有的字幕解析函数上。

---

## 二、P1 级：功能缺失或状态错误

### 5. ✅ 快捷面板的 sherpa 语言选择是死代码

**位置**：`vr/VRPlayerScreen.kt:1030`（开）／`:1063`（内嵌）／`:1165`（闭合）

```kotlin
if (asrEngineType == AsrEngineType.VOSK) {          // 1030 行开
    ... VoskLanguage.entries.forEach { ... }
    // ===== sherpa 引擎语言选择（v111：快捷面板同步）=====
    if (asrEngineType == AsrEngineType.QWEN3 ||     // 1063 行：永远为 false
        asrEngineType == AsrEngineType.SENSEVOICE_QNN) {
        ... SherpaAsrManager.sherpaLanguages.forEach { ... }
}  // 1165 行：end if (VOSK)
```

**问题**：两个条件互斥，第 1063 行的分支被嵌在"只有 VOSK 才进入"的分支里，**永不可达**。注释明说这是为了"快捷面板同步"，实际选 Qwen3／SenseVoice 引擎时该行不显示，用户只能去设置面板改语言。

**建议**：把 1063 行那块移到 1030 行的 `if` **之外**。

---

### 6. ✅ 切换解码设置会静默重置视角／立体／陀螺仪

**位置**：`vr/VRPlayerScreen.kt:2189`

```kotlin
// key 包含 isSoftwareDecoding 与 decoderEngine
LaunchedEffect(selectedMediaItem, photoReloadTrigger, isSoftwareDecoding, decoderEngine) {
    keepUiAlight()
    val view = currentGlSurfaceView ?: return@LaunchedEffect
    if (selectedMediaItem.isVideo) {
        // Default to 180° Dome, Left visual eye perspective, SBS 3D, and disable gyroscope when video opened
        projectionMode = ProjectionMode.VR_180
        domeHalfSelect = 1
        isGyroEnabled = false
        stereoMode = StereoMode.SBS
```

**问题**：effect 体内**无条件**重置投影/立体/陀螺仪（注释也写明这只应在"打开视频时"做一次），但 key 里混进了 `isSoftwareDecoding`（解码模式）和 `decoderEngine`（解码器引擎）。

**影响**：用户在播放中切换"硬件/软件解码"或换解码器引擎，正在看的 180°/360°/平面视角、手动选的立体模式、已开的陀螺仪**全部被重置**，且没有任何提示。`photoReloadTrigger`（容器修复、SPS 修复）触发时同样会跳掉。

**建议**：把"打开新视频时的一次性初始化"拆到独立 effect（key 只留 `selectedMediaItem`），解码设置变更走另一条只重建播放器的路径。

---

### 7. ✅ "恢复上次播放位置"逻辑语义混乱

**位置**：`vr/VRPlayerScreen.kt:1516`（定义）、`:2163`（使用）、`:2181`（写入）

```kotlin
// 1516：remember，整个 Composable 生命周期内持续存在，不按媒体区分
var restorePositionMs by remember { mutableLongStateOf(0L) }

// 2163：判断时 playerInstance 仍是"上一个已 release 的播放器"（新的在第 2169 行才赋值）
if (restorePositionMs > 0L && restorePositionMs < (playerInstance?.duration ?: Long.MAX_VALUE)) {
    seekTo(restorePositionMs)
}

// 2181：每 150ms 被实时覆盖
if (player.isPlaying && player.currentPosition > 0) {
    restorePositionMs = player.currentPosition
}
```

**问题**（两层）：
1. **语义名不副实**：`restorePositionMs` 被 2181 行每 150ms 写成**当前播放位置**，它根本不是"上次播放位置"，而是"当前位置"的镜像。
2. **不按媒体区分 + 边界判断用错对象**：该变量跨媒体共享；`2163` 行的边界检查读的是**旧播放器**（此时 `playerInstance` 尚未指向新建的 `exo`），其 `duration` 在 release 后为 `C.TIME_UNSET`（负数），条件恒假——也就是说这个 seek **实际几乎不会执行，但原因纯属巧合**，依赖了"已 release 的播放器返回负 duration"这一未声明的行为。

**影响**：位置恢复功能**不按设计工作**；一旦 ExoPlayer 未来版本改变 release 后 `getDuration()` 的行为，就会立即变成"新视频跳到上个视频的位置"。

**建议**：明确拆成两个变量（`lastSavedPositionMs` 按媒体 URI 存到 prefs；`currentPositionMs` 用于 UI），并把边界判断改用新建的 `exo`。

---

### 8. ✅ 内嵌字幕（ExoPlayer Cue）跨媒体残留

**位置**：`vr/VRPlayerScreen.kt:2133`（写）、`:2235` 起（换媒体的清理）

```kotlin
override fun onCues(cueGroup: androidx.media3.common.text.CueGroup) {
    if (cueGroup.cues.isNotEmpty()) {
        exoCueText = cueGroup.cues.joinToString("\n") { it.text ?: "" }
    } else {
        exoCueText = null
    }
}
```

**问题**：`exoCueText` 只在 `onCues` 回调里被赋值/清空。切换到**图片**时播放器被 `release()`（`2215` 行），不会再触发 `onCues(empty)`；切换到**没有内嵌字幕轨**的新视频同理。而换媒体的清理 effect（`2235` 起）只清了 `loadedSubtitleCues` 和 `loadedSubtitleFileName`，**没有清 `exoCueText`**。

**影响**：上一个视频的最后一句内嵌字幕会一直停留在屏幕上（`SubtitleOverlay` 在 `subtitleCues` 为空时以 `exoCueText` 兜底显示）。

**建议**：在 `2235` 的清理 effect 里补 `exoCueText = null`。

---

### 9. ✅ 传感器监听器在退出页面时不注销（资源泄漏）

**位置**：`vr/VRPlayerScreen.kt:2274-2280`（onDispose）、`:2308-2313`（启停）

```kotlin
DisposableEffect(Unit) {
    onDispose {
        playerInstance?.release()
        playerInstance = null
        currentGlSurfaceView?.release()
        // ← 缺少 sensorManager?.stop()
    }
}

LaunchedEffect(isGyroEnabled, sensorManager) {
    if (isGyroEnabled) sensorManager?.start() else sensorManager?.stop()
}
```

**问题**：`VRSensorManager.start()` 会执行 `sensorManager.registerListener(this, ...)`（`VRSensorManager.kt:50-54`），而 `VRSensorManager` 持有 `context`（Activity）与 `renderer`（`VRSensorManager.kt:12`）两个强引用。`DisposableEffect.onDispose` 没有调用 `stop()`。

注意：`LaunchedEffect` 的 `else` 分支**只在 `isGyroEnabled` 变为 false 时**才执行 `stop()`；composable 离开页面时协程被取消，**不会**走 `else`，也不会执行任何回滚。

**影响**：只要用户开过陀螺仪再退出/旋转屏幕，`SensorEventListener` 会持续持有 Activity 与 GL 上下文 → 稳定可复现的内存泄漏 + 后台耗电。

**建议**：`onDispose` 里补 `sensorManager?.stop()`。

---

## 三、P2 级：稳定性与健壮性（建议修，但触发条件较窄）

### 10. ✅ 畸形码流导致无限循环（挂死）

**位置**：`vr/ExperimentalDecode.kt:247-251`

```kotlin
fun readUe(): Int {
    var zeros = 0
    while (readBit() == 0) zeros++        // ← readBit 在流耗尽后恒返回 0
    return if (zeros == 0) 0 else ((1 shl zeros) - 1) + readBits(zeros)
}
```

**问题**：`readBit()` 在越界后（`:232-234`）会置 `ended = true` 并**返回固定值 0**。因此当解析到一个已耗尽的码流时，`while (readBit() == 0)` 永远为真，`zeros` 无限自增，**循环永不退出**。

**触发**：`rewriteHevcSps` / `rewriteAvcSps` 用于解析任意用户视频的 csd-0（本地／SMB／在线来源）。遇到 SPS 被截断、或 `sps_max_sub_layers_minus1 > 0` 导致解析错位，就会在调用线程上死循环挂死。

**建议**：`readBit()` 返回一个哨兵值（如 `-1`）或让 `readUe` 检查 `ended`；同时给 `zeros` 加上上限（如 `> 32` 即中止）。

---

### 11. ⚠️ `glReadPixels` 行序导致人脸位图上下颠倒

**位置**：`vr/VRGLRenderer.kt:1073-1085` 与 `:1433-1438`（子探查发现，未实测）

`glReadPixels` 的原点在**左下角**（第 0 行是屏幕底部），而 `Bitmap.setPixels` 的第 0 行是位图**顶部**，二者直接对接会把图像上下翻转。MediaPipe / 回退 `FaceDetector` 返回的 `centerY` 是位图坐标系，随后被当作 `uFaceCenter.y` 与 shader 中 `tc.y`（0 为图像顶部）比较，导致人脸特征纵向镜像。

**建议**：读取后逐行翻转，或把 `y` 换算为 `1.0 - y` 再喂给检测器。**修前建议真机对比验证**（OES 视频路径是否有隐性补偿翻转需实测确认）。

---

### 12. ⚠️ GL 资源释放在主线程调用

**位置**：`vr/VRGLRenderer.kt:1392-1395`，调用链 `VRPlayerScreen.kt:2278 → VRGLSurfaceView.kt:215-217`

```kotlin
if (lutTextureId != -1) {
    GLES20.glDeleteTextures(1, intArrayOf(lutTextureId), 0)   // ← 主线程调用
    lutTextureId = -1
}
```

`onDispose` 运行在 Compose 主线程，而 GL 上下文是 GL 线程 current 的。主线程没有 current 上下文，该调用**不会真正删除纹理**，只会产生 GL 错误。同一函数还漏删了 `videoProgram/imageProgram/fallbackProgram` 与 `videoTextureId/imageTextureId/placeholderTextureId`。

**建议**：改为 `glSurfaceView.queueEvent { renderer.release() }`，并在其中补全所有 GL 对象的删除。

---

### 13. ⚠️ SurfaceTexture 在上下文重建时泄漏

**位置**：`vr/VRGLRenderer.kt:860-870`（子探查发现，未实测）

`VRGLSurfaceView` 未设置 `setPreserveEGLContextOnPause(true)`，因此每次后台/前台切换 EGL 上下文都会销毁重建，`onSurfaceCreated` 会对同一 renderer 实例再次执行，而这里直接覆盖 `videoSurfaceTexture` 字段，**旧实例没有被 `release()`**（`release()` 只在 Activity 彻底销毁时调用一次）。

**影响**：每次挂起/恢复泄漏一个 native SurfaceTexture。

---

### 14. ✅ `SmbDataSource` 违反 Media3 DataSource 契约

**位置**：`vr/SmbDataSource.kt:24-46`

```kotlin
override fun open(dataSpec: DataSpec): Long {
    ...
    if (dataSpec.position != 0L) {
        inputStream?.skip(dataSpec.position)     // 返回值被丢弃
    }
    transferStarted(dataSpec)
    return C.LENGTH_UNSET.toLong()               // 无条件返回 UNSET
}

override fun read(buffer: ByteArray, offset: Int, length: Int): Int {
    val read = stream.read(buffer, offset, length)   // 不做 position+length 边界截断
```

**问题**：
1. Media3 约定：当 `dataSpec.length != C.LENGTH_UNSET` 时，`open()` 返回值**必须等于该 length**；此处无条件返回 `UNSET`，有界请求会读到超出请求范围的数据。
2. `InputStream.skip()` 的契约允许"少跳"且返回值被丢弃，一旦少跳，后续读取从错误偏移开始（表现为花屏/错帧）。

**建议**：按 `dataSpec.length` 返回正确的长度，校验 `skip` 的实际返回值并在不足时循环补齐。

---

### 15. ⚠️ 在线字幕下载非原子写入

**位置**：`vr/OnlineSubtitleSearch.kt:94-118`（子探查发现）

两条分支都直接写入 `outFile` 最终路径，没有"写临时文件 + 原子 rename"，异常时也不删除半成品。下载中断／解压 CRC 失败／进程被杀时，会在最终路径留下不完整文件，而外层 `catch` 只返回 `null`，**文件仍在磁盘上**，后续重试可能把它当作完整字幕使用。

**建议**：先写 `outFile.tmp`，校验通过后再 `renameTo(outFile)`。

---

### 16. ⚠️ `VideoRemuxer` 固定 1MB 缓冲对 8K 素材可能不足

**位置**：`vr/VideoRemuxer.kt:133-149`

```kotlin
val buffer = ByteBuffer.allocate(1 shl 20)   // 固定 1 MB
```

该文件的核心用途正是处理 8K（8192×4096）素材，而 8K HEVC 的 I 帧很容易超过 1MB。代码既没有动态扩容，也没有在 `size` 超过缓冲容量时报错。

---

### 17. ⚠️ 分析统计关闭后无法重新开启

**位置**：`vr/AnalyticsManager.kt:46-47`、`:85-87`

`shutdown()` 只把 `firebaseAvailable` 置 false，**未复位 `initialized`**。因此"同意 → 拒绝 → 再同意"的序列中，第二次 `init()` 会在 46 行直接 return，`firebaseAvailable` 永远无法重新置 true，上报静默失效。

**建议**：`shutdown()` 中补 `initialized = false`。

---

## 四、P3 级：细节问题

### 18. ✅ SRT 时间戳受系统语言影响

**位置**：`vr/AsrBatchTranscriber.kt:592`

```kotlin
return "%02d:%02d:%02d,%03d".format(s / 3600000, ...)
```

Kotlin 的 `String.format` 使用**默认 Locale**。在阿拉伯语／波斯语等系统语言下，`%d` 会输出本地数字字形（如 `٠١:٢٣`），生成的 SRT 时间戳将无法被播放器解析。项目存在 `values-en`（有国际化意图），此问题应一并处理。

**建议**：改为 `String.format(java.util.Locale.US, ...)`。

### 19. ✅ 两处注释存在编码乱码

**位置**：`vr/VRPlayerScreen.kt:821`、`:4402`

```
821:  // v91: 鍚庡彴杞啓鍖哄潡锛堣缃潰鏉夸笌涓荤晫闈㈠瓧骞曞揩鎹烽潰鏉垮叡鐢級
4402: // ===== 鍚庡彴鐢熸垚鍏ㄧ墖瀛楀箷锛坴91 鎻愬彇澶嶇敤锛?====
```

典型的 GBK 字节被当作 UTF-8 解读后的乱码（原文应为「后台生成全片字幕（v91 提取复用）」之类）。全项目仅此 2 处，不影响功能，但建议修复以免误导后续维护。

### 20. ✅ 界面文案几乎全部硬编码中文

全项目 `Text("...中文...")` 形式的硬编码文案约 **94 处**，集中在 `VRPlayerScreen.kt`、`SubtitleSettingsPanel.kt`、`MainActivity.kt`。而 `res/values-en/strings.xml` 中**只有 `app_name` 一条**，等于英文适配实际未落地——英文系统上仍会整段显示中文（含首启强制弹出的隐私同意弹窗）。

---

## 五、审查结论

**整体判断**：架构清晰、注释密度高、核心数学（传感器四元数、几何生成）质量不错，没有 `GlobalScope`、没有 TODO 堆积。但**UI 层与渲染层的"接线完整性"和"环境边界"是明显短板**。

**问题分布特征**：

| 类型 | 数量 | 典型代表 |
|---|---|---|
| 功能静默失效（做了但没生效） | 4 | LUT、MediaPipe 精细特征、在线字幕加载、sherpa 语言 |
| 状态机／生命周期错误 | 5 | 解码切换重置视角、位置恢复、字幕残留、传感器泄漏 |
| 环境边界未处理 | 3 | API 29 方法、畸形码流死循环、Locale |
| 契约／健壮性 | 5 | SMB DataSource、非原子下载、8K 缓冲等 |

**最值得先修的 4 条**（改动小、收益明确）：**#1 LUT**、**#2 MediaPipe 特征**、**#3 API 级别**、**#4 在线字幕接线**。前两条属于"功能写了但用户永远看不到效果"，第 3 条是确定性崩溃。

**关于上一轮的 MP2 修复**：本次审查**未发现**上一轮软解改动引入的新缺陷。逐一复核了 `extractWithSoftwareMpegDecoder` 的解码/分块/重采样逻辑，并对 `SampleBuffer.getBufferLength()` 的语义做了本机实测——

```
bufferLength() = 2304, channelCount = 2  → MP2 单帧每声道 1152
```

确认它返回的是**交错总样本数**，故代码中 `bufferLength / channels` 取每声道帧数**是正确的**；`resampleLinear` 对空数组／单样本数组也安全；`ExtractorInputStream` 的缓冲语义与 MediaExtractor 契约一致。

**唯一与上一轮修复相关的隐患**：#18 的 `String.format` Locale 问题位于共用的 `writeSrt()`，非本次新增，但会影响生成的字幕文件在特定系统语言下的可用性。

---

## 附：未纳入本报告的排查项

以下方向经检查**未发现**值得报告的问题，列出以免重复排查：

- `vr/VRSensorManager.kt` — 四元数双覆盖处理、归一化、`getRotationMatrixFromVector` 入参顺序、`remapCoordinateSystem` 四向映射均正确，`start()/stop()` 有 `isListening` 对称保护
- `vr/GeometryHelper.kt` — `createFloatBuffer` 正确设置 `nativeOrder()`，`acos` 入参有 `coerceIn(-1.0, 1.0)`
- `vr/DemoMediaProvider.kt` — 纯 Bitmap/Canvas 绘制，未发现越界/除零/泄漏
- `AppApplication.kt` — 逻辑与注释一致
- 全项目**无** `GlobalScope`、**无** `TODO/FIXME` 堆积

---

## 六、v1.0.117 修复记录（2026-09-13）

以下 4 条 P0 与 P1 的 #6 已修复，`assembleDebug` / `assembleRelease` 均 BUILD SUCCESSFUL，并在 MuMu 模拟器上实测。

| # | 问题 | 修改文件 | 修复方式 |
|---|---|---|---|
| 1 | 全部 LUT 静默失效 | `LutUtils.kt:50-75` | 关键字截取统一改用小写化的 `lower`（`.cube` 标准写法是大写 `LUT_3D_SIZE`），并把 `toInt()` 换成 `toIntOrNull()` 兜底 |
| 2 | MediaPipe 精细特征从未生效 | `MediaPipeFaceManager.kt:129-145` | 构造 `FaceResult` 时补传 `hasDetailedLandmarks = true` |
| 3 | Android 7/8/9 必然崩溃 | `DecoderCapabilities.kt:23-40`、`ExperimentalDecode.kt:40` | 新增 `DecoderCapabilities.isHardwareAccelerated(info)`；API ≥ 29 走官方方法，API < 29 走编解码器名称启发式（`omx.google.` / `c2.android.` / `.sw.` / `software` 视为软解） |
| 4 | 在线字幕"已下载并加载"是假提示 | `VRPlayerScreen.kt:4431+` | 调用点补传 `onSubtitleFileLoaded`（解析 SRT → 置入 cue 列表 → 自动开启字幕 → 触发翻译）与 `maxLines` / `onMaxLinesChange` |
| 6 | 切换解码设置会重置视角／立体／陀螺仪 | `VRPlayerScreen.kt:2188-2255` | 把「媒体绑定」与「打开视频时的默认视角初始化」拆成两个 effect：抽出局部函数 `rebindCurrentMediaToRenderer()` 只负责绑定；Effect A（key = `selectedMediaItem`）做一次性默认视角；Effect B（key = `isSoftwareDecoding` / `decoderEngine` / `photoReloadTrigger`）只重建播放器并 seek 回原进度，**完全不触碰视角** |

### 实测证据（MuMu 模拟器，v1.0.117 debug）

- **LUT**：进入"高级播放参数与美肤协同微调 → LUT 视频滤镜"，点击「经典青橙」→
  日志输出 `VRGLRenderer: LUT texture uploaded (1048576 bytes)`（1048576 = 512×512×4，正是 LUT 纹理尺寸），
  **无** `NumberFormatException` / `LUT xxx load failed`；按钮高亮，底部文案变为「当前：经典青橙」。
  修复前该路径必然抛 `NumberFormatException` 并静默失败。
- 12 个内置 `.cube` 数据行数均为 **35937 = 33³**，与 `LUT_3D_SIZE 33` 一致，可稳定通过 `require` 校验。
- 应用冷启动无崩溃，`MediaPipeFaceManager: Google MediaPipe FaceLandmarker (VIDEO mode) successfully initialized!`。
- **#6 切换解码不重置视角**（复现步骤实测）：
  1. 设置 → 镜头投影与视角 → 视角格式改选「标准平面」（画面切为平面，两侧出现经度刻度，右上提示「当前 2D 模式（标准平面）」）
  2. 设置 → 解码内核与帧率 → 解码模式点「软件解码（CPU 兼容）」，Toast 提示「已切换为：软件解码」
  3. 回到「镜头投影与视角」→ 视角格式**仍为「标准平面」高亮**，画面仍为平面，提示文案未变
  —— 修复前该 effect 的 key 含 `isSoftwareDecoding`，此处必然把投影重置回 `VR_180`，画面跳回球面。

### 尚未修复（后续可继续）

P1 #5、#7、#8 与 P2 #10–#17、P3 #18–#20 保持原状，其中建议优先处理 **#7 位置恢复语义混乱**、**#8 内嵌字幕跨媒体残留**（#9 传感器泄漏已在陀螺仪专项中一并修复，见 `VRSensorManager` 改动记录）。

### 备份

本轮所有被修改文件均在原目录留有备份：`<原名>.20260913-002440.bak`
（`app/build.gradle.kts` 为 `.20260913-002359.bak`，本报告为 `.20260913-003012.bak`）。

## 七、第七轮：SenseVoice QNN 修复（v1.0.118，2026-09-13）

用户要求"SenseVoice QNN 修复"。诊断发现 QNN 路径存在**两个必然失败**的根因，另有 SoC 硬编码问题：

### 根因

| # | 问题 | 证据 |
|---|---|---|
| 1 | **从未设置 `ADSP_LIBRARY_PATH`** | 全项目 grep `adsp`/`prepend` 零命中。sherpa-onnx 的 `PrependAdspLibraryPath`（jni/common.cc）注释明确：不设置时 `qnn_interface.deviceCreate()` 返回错误码 1008，HTP DSP 无法加载 `libQnnHtpV*Skel.so`。aar 已导出 `OfflineRecognizer.prependAdspLibraryPath()` 但项目从未调用 |
| 2 | **QNN 运行库所在目录为空** | targetSdk=36 下 AGP 默认 `extractNativeLibs=false`，实测设备 `/data/app/.../lib/arm64/` **完全为空**；而代码用 `File(nativeLibraryDir, "libQnnHtp.so").absolutePath` 拼路径，dlopen 必失败。15 个 `libQnn*.so`（约 140MB）虽在 jniLibs 里，但从不落盘 |
| 3 | **SoC 硬编码 SM8850** | 官方 release（asr-models-qnn-binary）实际提供 SM8550/8650/8750/8850 等 10 款 SoC 的 context binary；写死 SM8850 使其它骁龙机型即使修好 1/2 也无法使用 |

### 修复

- `app/build.gradle.kts`：`packaging { jniLibs { useLegacyPackaging = true } }` —— so 解压落盘。**附带收益：APK 从 419MB 降到 212MB**（so 由未压缩变为 Deflate 压缩存储），release 从 410MB 降到 191MB
- `SherpaAsrManager.kt`：
  - 新增 `prepareQnnRuntime()`：校验 `libQnnHtp.so`/`libQnnSystem.so` 等在 `nativeLibraryDir` 落盘 → 调用 `OfflineRecognizer.prependAdspLibraryPath(libDir)`（官方 demo 同款顺序：必须先于识别器创建）
  - 新增 `deviceSocModel()`（`Build.SOC_MODEL` → `Build.HARDWARE` 兜底）与 `QNN_SUPPORTED_SOCS` 官方 10 款列表；模型目录名按检测到的 SoC 动态拼接（`senseVoiceModelDirName()`）
  - 模型下载 URL/目录/体积全部参数化（5 秒档 int8，~161MB）；`isModelReady` 加最小文件尺寸校验（防 0 字节残留误判就绪）
  - `lastInitError` 状态：初始化失败原因透出到 UI，不再一律谎报"模型未下载"
  - SoC 不在支持列表时：下载入口直接替换为红色警告"当前设备（xxx）无官方 QNN 模型，请改用 Qwen3-ASR"，阻止白等 161MB
- `AsrBatchTranscriber.kt`：失败提示改为优先显示 `SherpaAsrManager.lastInitError`
- `VRPlayerScreen.kt`：清除 4 处硬编码 "SM8850" 文案，改为"SoC 自动匹配"
- 升版 `versionCode 118 / versionName 1.0.118`

### 实测证据（MuMu 模拟器，v1.0.118 debug）

- 安装后 `dumpsys` 显示 `extractNativeLibs=true`，`/data/app/.../lib/arm64/` 从**空目录**变为 **30 个 so 齐全**（libQnnHtp.so / libQnnSystem.so / libQnnHtpPrepare.so / libQnnHtpV79Stub.so / libsherpa-onnx-jni.so 等，约 150MB 落盘）
- APK 内 so 由 `Stored` 变为 `Defl:N` 压缩存储，APK 体积 419MB → 212MB
- MuMu（模拟器，`ro.soc.model` 为空 → 检测结果 "unknown"）上选择 SenseVoice 引擎：UI 正确显示 "SenseVoice QNN **unknown**"，下载按钮替换为红色警告"当前设备（unknown）无官方 QNN 模型，请改用 Qwen3-ASR" —— SoC 防呆分支生效
- 应用冷启动无崩溃
- **限制说明**：MuMu 模拟器无骁龙 NPU/Hexagon DSP，QNN 推理端到端只能在 SM8550/8650/8750/8850 真机上验证；模拟器可验证的环节（库落盘、ADSP 路径设置、SoC 检测、防呆 UI）均已通过

### 本轮备份

`app/build.gradle.kts`、`SherpaAsrManager.kt`、`AsrBatchTranscriber.kt` → `*.20260913-010107.bak`；
`VRPlayerScreen.kt` → `*.20260913-011838.bak`（取自 v117 git 提交版本）；报告与记忆另见同目录时间戳备份。
